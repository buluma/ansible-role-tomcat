---
- name: deploy lib
  ansible.builtin.get_url:
    url: "{{ lib.url }}"
    dest: "{{ tomcat_directory }}//{{ instance.name }}/lib/"
    mode: 0755
  when:
    - lib.url is defined
  notify:
    - restart tomcat instance
